import { Image, StyleSheet, Platform, View } from 'react-native';
import Onze, {Cabecalho, Corpo, Rodape} from '@/components/Onze';
import El from '@/components/El';


export default function HomeScreen() {
  return (
    <View style={{backgroundColor:'', flex: 1, marginBottom: 20, padding: 10, borderRadius: 8}}>
      
      <El />
    </View>
  );
}


